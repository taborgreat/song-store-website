extends layout

block content

  h1= title

  ul
    each song in song_list
      li 
        a(href=song.url) #{song.title}
        a(href=artist.url) #{song.artist}
        | key: #{song.key} bpm: #{song.bpm}
        a(href=genre.url) #{song.genre}


    else
      li There are no songs.

